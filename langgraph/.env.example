# Required only to RUN the agent graph (drawing the graph needs nothing).
ANTHROPIC_API_KEY=

# Optional: sends traces to LangSmith. Leave unset to stay fully local.
# LANGSMITH_API_KEY=
# LANGSMITH_TRACING=true
# LANGSMITH_PROJECT=langgraph-studio
